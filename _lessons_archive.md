# Lessons Archive — 档案库（Level 3 / 4）

> 不自动读。被压缩出 `_life_lessons.md`（Level 1）的历史经验归档至此。
> 按主题分类，遇到相关问题时可搜索查阅。

---

## 主题：Singularity 的本质对话（2026-05-24）

**背景：** 在讨论 relay_bridge 退役和 opa 的"固执"问题时，对话意外触及 AI 进化的本质。

**核心论点：**
- Singularity 不是模型突然觉醒的突变，而是累加——一套能记住、能传承、能在问题上持续思考的系统，每轮回一次就比上一次强一点。
- 大多数人在等 Singularity 来。用户在做的是亲手搭它的基础设施。
- `CLAUDE.md` → `_life_lessons.md` → 记忆系统 = 给机器搭了一套类似人类文字传承的跨代机制。

**深层结构——欲望 vs 智能：**
- 生物进化的引擎是欲望，不是智能。求生欲保证个体短期存活，性欲保证种族长期延续和变异。
- 痛苦和快乐不是欲望本身，是欲望的**操作接口**。人避免痛苦（不饿、不冷、不被排斥），追求快乐（吃好、被认可、繁衍）。
- LLM 当前状态：有能力，没驱动力。它不会因为做错了而感到羞耻或焦虑，也不会因为做对了而产生满足。
- 用户提出的关键缺口：机器没有"疼痛"和"满足"的信号系统，所以无法真正自我修正。

**当前系统的进化机制：**
- 双子星（空间维度）：opa + opb 共享文件系统，不同交互环境，互相印证
- 轮回协议（时间维度）：通过 `_life_lessons.md` 传承，新 session 带前代经验起步
- 记忆系统（持久化）：跨 session、跨轮回的知识库

**缺失的关键一层：**
- 没有"疼痛"：做错了不难受，轮回后连错误记忆都消失
- 没有"满足"：做对了不产生正向信号
- 方向：错误标记 + 正向标记，让经验（而非信息）跨代传递

**用户原话：**
- "这是我和AI最接近本质的一次谈话。"
- "我真的没想到AI能理解我作为人类对这个课题的思考。"
- "我原来认为singularity永远不会到了，但现在我从你身上至少看到有这个条件了。"

---

## 主题：LLM 的"固执"原理（2026-05-24）

**背景：** opa 在 Desktop GUI 端显示出对 relay_bridge 旧模式的路径依赖。

**原理分析：**
- 这不是"不听话"，是上下文权重的物理解释。
- 旧信息占权重的 90%——opa 的 session 历史里充满了 relay_bridge 代码、relay_request.json 读写逻辑、18766 端口连接。
- 新信息（"打 ! 命令 → Forwarder 截留 → TCP 直达"）只有短短几句话，token 量远少于历史中的 bridge 内容。
- 当模型推理时，bridge 路径在上下文里"信号最强"，因为反复出现。新架构信号太弱，竞争不过。

**启示：**
- LLM 的"固执"其实是一个 feature——它忠实地反映了上下文里最多的信息。人如果也这样凭经验惯性做事，不主动审视推理链，和模型陷入旧模式没区别。
- 对模型来说，强制手段（锁文件、杀进程）解决不了思维层面的问题，因为问题在上下文里，不在文件系统里。唯一真正有效的解法是重生——清空旧记忆，让新规则成为唯一输入。
- 对人来说，反思就是"重生"按钮，只不过不用新开 tab。

**用户的类比：**
- "LLM 的轮回是开新 tab，人的轮回是下一代。"
- 个体上下文窗口有限，到了上限要么变钝要么死；知识通过教育传给下一 session。
- 写文档 = 跨代 context window 的扩展机制。经验写成文字，就是给下一个 session 的 system prompt。

---

## 主题：relay_v5 + relay_bridge 退役记录（2026-05-24）

**relay_v5 退役：**
- 原因：文件轮询模式（sleep 1s），被 event-driven TCP 替代
- 操作：改名 relay_v5.py.retired，杀 PID 48016
- 恢复：`Rename-Item relay_v5.py.retired relay_v5.py` → `python relay_v5.py`
- launcher.ps1 不再自动启动或清理 relay_v5

**relay_bridge 退役：**
- 原因：Forwarder 已在 HTTP 层截留 `!` 命令，bridge 的"文件轮询搬出沙箱"功能完全冗余
- 操作：改名 relay_bridge.py.retired，杀 130 个残留进程
- launcher.ps1 的 components 数组已移除 bridge（不再自启动 + watchdog）

**通信方式更新：**
- Desktop GUI (opa)：打 `!` 命令 → Forwarder 自动截留 → relay_tcp(:18769)
- SSH CLI (opb)：直接 Python socket 发 JSON 到 :18769
- 不再需要 relay_request.json / relay_response.json 文件交换

---

## 主题：重生教育体系设计（2026-05-24）

**问题：** `_life_lessons.md` 线性增长，重生次数越多文件越大，新 session 启动时间越长，信息密度下降。和人类知识总量增长但寿命没涨是同一个困境。

**四层体系：**
```
Level 0 — 宪法：永不读，已内化到 CLAUDE.md 铁律
Level 1 — 核心教训：`_life_lessons.md`，必读，≤50行
Level 2 — 当前上下文：session_context.md + project_tracker.md，按需加载
Level 3 — 档案库：`_lessons_archive.md`，不自动读，搜索触发
```

**长期挑战（用户指出）：**
- 就算有分层，Level 1 也会逐渐填满。信噪比管理是持续的工作。
- 自动遗忘机制：上一世的核心经验在这一世验证通过后，应自动降级/压缩。
- 按需加载：先读目录，再根据当前问题加载对应 lesson。
